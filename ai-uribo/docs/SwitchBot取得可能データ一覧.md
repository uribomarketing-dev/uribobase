# SwitchBotから拾えるデータ一覧（AI Uribo接続の検討用）

調査日：2026-08-12／出典：SwitchBot公式API v1.1ドキュメント（デバイス別ページを直接確認）

**この一覧の使い方**：左から順に「どの機器が」「何を返すか」「AI Uriboのどの項目を埋められるか」。
埋められる＝**その項目は人に質問しなくなる**、ということです。推定で埋めたものは「要精査」が付きます。

---

## 0. 取得の2経路（両方使える）

| 経路 | 仕組み | 向いていること | 制約 |
|---|---|---|---|
| **ポーリング** | GAS から `GET /v1.1/devices/{id}/status` を叩いて現在値を取る | 温湿度・電力・施錠状態など「今どうなっているか」 | 1トークンあたり1日10,000リクエストが上限（要確認）。1日数回なら余裕 |
| **Webhook** | 変化した瞬間にSwitchBot側からURLへPOSTされる | 開閉・人感・施錠など「いつ起きたか」＝**時刻が要る記録** | 設定できるURLの数に制限あり（要確認）。AI UriboのWebhook URLをそのまま使えます |

**時刻が要るもの（開閉・人感・施錠・服薬ボックス）はWebhook**、
**状態が要るもの（温湿度・照度・電力）はポーリング**、という使い分けが基本になります。

---

## 1. センサー系（実態把握に直結）

| 機器 | 返ってくるデータ（実際のフィールド名） | Webhook | AI Uriboで埋められる項目 | 確度 |
|---|---|---|---|---|
| **開閉センサー**（Contact Sensor） | `openState`（open/close/timeOutNotClose）、`moveDetected`（人の動き true/false）、`brightness`（bright/dim）、`battery` | ○ `detectionState`（DETECTED/NOT_DETECTED）、`doorMode`（IN_DOOR/OUT_DOOR）、`openState`、`timeOfSample`（時刻） | **在否確認**／**外出・帰宅時間**（開いた時刻・戻った時刻）／**夜間の動き**／服薬ボックスに付ければ**服薬確認** | 高 |
| **人感センサー**（Motion Sensor） | `moveDetected`（true/false）、`brightness`（bright/dim）、`battery` | ○ `detectionState`、`timeOfSample` | **夜間巡回・就寝確認**（居室で動きあり）／**在否確認**／起床・就寝時刻の推定 | 中〜高 |
| **人感センサー Presence**（mmWave） | `Detected`（true/false）、`lightLevel`（1〜20）、`battery` | ○ `detectionState`、`lightLevel` | 上に加え、**在室の継続検知**（動かなくても居るのが分かる＝就寝中の在室確認に強い）、`lightLevel`で**消灯＝就寝／点灯＝起床**の推定 | 高 |
| **温湿度計**（Meter / Meter Plus / Outdoor Meter） | `temperature`（℃）、`humidity`（%）、`battery` | ○ 同上＋`scale`（CELSIUS）、`timeOfSample` | **居室環境の記録**（新設候補：室温・湿度）。熱中症・低体温のリスク把握 | 高（実測値） |
| **温湿度計 Pro CO2** | 上記＋`CO2`（0〜9999ppm） | ○ | 上記＋**換気の記録**（新設候補）。CO2上昇＝換気不足の材料 | 高 |
| **漏水センサー**（Water Leak Detector） | `status`（0=dry / 1=leak） | ○ `detectionState`（0/1） | **緊急時・異常時対応**への自動通報（漏水を検知したら社員へLINE） | 高 |

## 2. ハブ・照度

| 機器 | 返ってくるデータ | Webhook | AI Uriboで埋められる項目 | 確度 |
|---|---|---|---|---|
| **Hub 2** | `temperature`、`humidity`、`lightLevel`（1〜20の照度） | ○ | **居室環境**／照度から**消灯＝就寝・点灯＝起床**の推定／夜間の点灯＝**不眠・トイレ覚醒**の材料 | 中〜高 |
| Hub 3 / Hub Mini / AI Hub | 機種により温湿度・照度（Hub Miniは赤外線中継のみ） | 機種による | 同上 | — |

## 3. 施錠・入退室

| 機器 | 返ってくるデータ | Webhook | AI Uriboで埋められる項目 | 確度 |
|---|---|---|---|---|
| **スマートロック**（Lock / Lock Pro / Ultra） | `lockState`（LOCKED/UNLOCKED/JAMMED）、`doorState`（open/closed）、`calibrate`、`battery` | ○ `lockState`、`timeOfSample` | **戸締まり確認**（新設候補・夜間巡回の裏付け）／**外出・帰宅時間**／`JAMMED`は設備異常として通報 | 高 |
| **キーパッド／指紋**（Keypad 各種） | 解錠イベント | ○ | **誰がいつ出入りしたか**（本人・職員・家族の別を登録すれば来訪記録にも） | 高 |
| **ビデオドアベル** | 来訪イベント | ○ | **家族・関係機関との連絡**の裏付け（来訪時刻） | 中 |

## 4. 電力・家電（生活リズムが見える）

| 機器 | 返ってくるデータ | Webhook | AI Uriboで埋められる項目 | 確度 |
|---|---|---|---|---|
| **プラグミニ**（JP） | `weight`（消費電力W）、`voltage`、`electricCurrent`（mA）、`electricityOfDay`（その日の通電分数） | ○ `powerState`（ON/OFF） | 何に挿すかで変わります：**電気ポット→起床**／**テレビ→在室・日中の過ごし方**／**電気毛布・エアコン→就寝環境**／**洗濯機→生活行為の記録** | 中（何に挿すか次第） |
| **ボット**（Bot） | `power`（on/off）、`deviceMode`、`battery` | ○ | 押した記録＝**特定の行為の実施記録**（例：給湯ボタン→入浴の推定） | 中 |
| **リレースイッチ 1PM/2PM** | 電力計測付きスイッチ | ○ | プラグミニと同様（配線機器の使用記録） | 中 |

## 5. その他（あれば拾える）

| 機器 | 返ってくるデータ | AI Uriboで埋められる項目 |
|---|---|---|
| **ロボット掃除機** | `workingStatus`、`onlineStatus`、`battery` | **環境整備の実施記録**（居室清掃を行った日） |
| **カーテン / ロールシェード** | `slidePosition`、`moving` | 開けた時刻＝**起床**、閉めた時刻＝**就寝**の推定 |
| **加湿器・空気清浄機・サーキュレーター** | 運転状態・設定 | 居室環境（乾燥対策の実施） |
| **シーリングライト・電球・テープライト** | 点灯/消灯・明るさ | 消灯＝**就寝**、深夜の点灯＝**覚醒**の推定 |

---

## 6. Uriboで今すぐ効きそうな順（費用対効果）

1. **服薬ボックスの開閉センサー（既存）** — 服薬確認。**すでに稼働中**
2. **玄関の開閉センサー or スマートロック** — 在否確認と**外出・帰宅時間**が丸ごと自動になる。
   A1（在否）とB2（外出・帰宅）の2項目が消えるので、質問削減の効果が最大
3. **居室の人感／Presenceセンサー** — 夜間巡回・就寝確認の裏付け。
   夜勤さんが一晩に何度も見に行かなくても「動きがあった／なかった」が残る
4. **Hub 2（照度つき）** — 消灯・点灯から**就寝・起床の時刻**が推定できる。室温・湿度も同時に取れる
5. **温湿度計** — 居室環境。夏冬の体調変化を「暑かった／寒かった」ではなく数字で残せる
6. **プラグミニ（電気ポット等）** — 起床・生活リズム。安価で効く

## 7. これを入れると新しく作れる記録項目（提案）

現在のS3チェック項目にない、**機械だけで埋まる**項目です。人への質問は増えません。

| 新項目（案） | 埋まる元 | 何の役に立つか |
|---|---|---|
| 起床時刻・就寝時刻 | 照度・カーテン・人感 | 生活リズムの変化＝体調・精神状態の予兆をつかむ |
| 居室環境（室温・湿度） | 温湿度計・Hub2 | 熱中症/低体温の予防。「暑い日に体調を崩した」の裏付け |
| 戸締まり確認 | スマートロック | 夜間の安全確認の記録 |
| 夜間の覚醒回数 | 人感・照度・開閉 | 不眠・頻尿の把握。支援の見直し材料 |
| 外出・帰宅時刻 | 玄関の開閉・ロック | B2が自動化。無断外出の早期把握にも |
| 設備異常 | 漏水・ロックのJAMMED | 発生した瞬間に社員へLINE |

---

## 8. 実装するとしたら（技術メモ）

- **認証**：v1.1はトークン＋シークレットで署名（HMAC-SHA256）を作る方式。
  GASの `Utilities.computeHmacSha256Signature()` で作れるので**追加ライブラリなしで実装可能**
- **ポーリング**：`GET /v1.1/devices` で機器一覧 → `GET /v1.1/devices/{deviceId}/status` で現在値。
  朝バッチの前（例：9:50）に1回走らせてS4に生ログを書けば、そのまま自動充足に乗ります
- **Webhook**：SwitchBot側に登録するURLは、AI Uriboの `?k=` 付きURLをそのまま使えます。
  受け口（doPost）で「LINEからの `events`」か「SwitchBotからの `eventType`」かを見分けて振り分ける形になります
- **秘密情報**：トークン・シークレットはスクリプトプロパティに保存（`SWITCHBOT_TOKEN` / `SWITCHBOT_SECRET`）
- **電池残量**：多くの機器で `battery` は 10/20/60/100 の4段階。**電池切れの予兆通知**にも使えます

## 9. 実装前に教えていただきたいこと

1. **いま何が何台あって、どこに付いているか**（機器名・設置場所・対象の利用者）
2. SwitchBotアカウントは拠点ごとに分かれているか、1つにまとまっているか
3. 上の「今すぐ効きそうな順」のうち、**追加してよいもの**

1が分かれば、その日のうちに取り込み（ポーリング＋Webhook受け口）を実装できます。
機器が増えても、`src/autofill.gs` の `AUTOFILL_SOURCES` に1件足すだけで対応できる作りにしてあります。

---

### 出典

- [SwitchBot Open API 公式ドキュメント](https://github.com/OpenWonderLabs/SwitchBotAPI)
- 各デバイスのフィールドは公式の device 別ドキュメント（contact-sensor / motion-sensor / presence-sensor /
  meter-pro-co2 / water-leak-detector / hub-2 / plug-mini-jp / lock）を直接確認しています
- 機種によって取れる項目が違うため、**実機のモデル名が分かった時点で再確認**してください
