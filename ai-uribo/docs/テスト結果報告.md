# テスト結果報告

日付：2026-08-12
対象：AI Uribo Phase0＋Phase1（`ai-uribo/src/` 全11ファイル）

---

## 1. 実施したこと

Google環境・LINE実機が無い状態でも論理を検証できるよう、**GASのAPI（スプレッドシート・
LINE送信・プロパティ・キャッシュ・ロック・トリガー・Drive）をメモリ上に再現したテストハーネス**を作り、
実際のソースコードをそのまま読み込んで動かしました。

```bash
cd ai-uribo/tests && node harness.js
```

**結果：116項目すべてOK（NG 0件）**。実行ログ（S10）にも想定外のエラー0件。
※初回53項目 → 別AIレビュー（第1回）の指摘対応で17項目追加。

## 2. 検証できたこと（自動テスト116項目）

| 区分 | 確認内容 |
|---|---|
| 登録 | 登録コード方式（氏名一覧は出さない／使い捨て／使用済みコードで乗っ取れない／無効スタッフのコードは拒否） |
| 排他制御 | ロックを取れないときは処理せず案内（シートを書き換えない・バッチはスキップ） |
| 権限 | 他人あてのタスクには回答できない／無効化されたスタッフの操作を拒否／夜勤の「状況」は自分の担当分のみ |
| 台帳生成 | S1〜S12の12シート生成／初期データ投入／**再実行しても壊れない（冪等）** |
| 朝バッチ | 自動充足 → 不足検出 → S5登録 → 見出し＋最初の質問をLINE送信 |
| 送信先 | Stage1は**藤原様・服部様の2名にまとめて送信**（【08】どおり） |
| 未登録者 | line_user_id未登録（兼崎様）には送信されず警告ログのみ |
| 回答 | ボタン → S6記録 → S5完了 → S7補完台帳＋S4へ転記 → 次の質問が届く |
| 一言記述 | 「未実施だった」等で追記を求め、追記後に記録として成立 |
| わからない | 完了にせず「エスカレーション中」で残る（04共通ルール） |
| 冪等性 | **同じ不足を二重検出しない／同じ質問を二重送信しない**（朝バッチ再実行で確認） |
| 二重回答 | 回答済みタスクの再タップを拒否。2名に送った項目は**先着1名で完了**し、もう一方が押しても二重記録しない |
| 自動充足 | センサー・出勤情報・前夜の予定から**推定できるものまで積極的に埋め**、その分は質問されない。推定分は「要精査」の印が付き、`精査` コマンドで一覧できる |
| 夜の確認セット | 明日の予定（R04）を検出し、夜勤（不在時は社員）へ送信。シフト希望は混ざらない |
| 深夜帯 | 22:00〜7:00は送信せずS6にキュー保存 → `flushQueue()` で朝に送信 |
| 週次ダイジェスト | 未完了一覧＋わからない率・自動充足率を社員へ送信、S10に週次集計を記録 |
| バックアップ | 全12シートのCSV出力・snapshot複製・復旧手順書の同梱 |
| コマンド | 状況／ヘルプ／報告／診断／想定外テキストの応答（診断は社員のみ・個人情報を含めない） |
| セキュリティ | 秘密キー未設定なら全拒否・不一致は破棄／未登録ユーザーの操作を拒否／重複イベントIDを無視／LINEの409を送信済みとして扱う |
| 監査対応 | その日の夜勤担当者を拠点ごとに1問だけ聞き、同じ日の全記録に「支援担当者：〇〇／管理者確認：〇〇」として残る。センサー由来の値には出所を明記 |
| 外部連携 | AIハブ（OpenClaw）等からの観察をPOSTで取り込み、推定として充足（映像・画像は受け取らない） |
| AIまとめ | LINEで貼り付けると対象日を読み取って取り込み、検出キーワードで該当項目を推定充足 |
| AI文章の扱い | 注意語は断定せず「誤りが多い」と明示して通知し、事実/誤検知/判断できないをボタンで回収。誤検知率を週次に表示 |
| SwitchBot連携 | 署名生成・機器読込（用途の自動推測・確認前は無効）・状態取得・電池警告・Webhook受信（MACで利用者に紐付け／未登録機器は拒否）・Webhook自動登録 |
| トリガー | `installTriggers()` で5本（朝10・夜21・日曜10・バックアップ3・キュー7）登録 |
| テストモード | 設定作業中は`test_mode`をONにしておくとLINEに実際には送らず、送るはずだった内容だけS6に「テスト」として残る。診断にもON/OFFが出る。OFFに戻すと通常どおり送信される |

## 3. 実装AI自身のテストで見つけて直した不具合（4件）

| # | 症状 | 原因 | 対応 |
|---|---|---|---|
| 1 | 週次ダイジェストの未完了が常に0件 | 集計期間を「対象日」で切っていたため、対象日が翌月になるシフト希望の不足が範囲外に落ちていた | 集計期間を「検出日時」で切るよう変更（`batch.gs`） |
| 2 | 夜の確認セットにシフト希望の質問が混入 | 絞り込みが「確認先役割=本人」だけで、対象種別を見ていなかった | support/planに限定（`batch.gs nightBatch()`） |
| 3 | 2名に送った項目で、後から押した方が二重記録される | 送信済みタスクは中止できないのに、完了済み不足のチェックが無かった | 完了済みなら記録せず「他の方が回答済み」と案内（`ask.gs handleAnswer_()`） |
| 4 | 送信失敗の再送が無限に繰り返される可能性 | 再送回数が毎回上書きされ累積しなかった | 累積カウントに変更（`notify.gs flushQueue()`） |

## 4. このテストでは検証できないこと（人間の確認が必須）

以下は実環境が要るため **`docs/テスト手順書.md` に沿って藤原様に実施いただく必要があります**。

1. LINEの実機表示（ボタンの見え方・文字切れ・クイックリプライの挙動）
2. LINE Developers のWebhook「検証」成功と、実際のイベント受信
3. Google認可フロー、スクリプトプロパティの読み出し
4. Driveの実フォルダ生成・CSVの文字化け有無（Excelで開く）
5. 時間主導トリガーが実際に定時に発火するか
6. スプレッドシートの同時編集・実行時間（6分制限）に対する実データ量での余裕
7. Mac→NASの週次同期（`mac/sync_to_nas.sh` の実行）

## 5. 別AIレビュー（第1回・技術面）

ChatGPTによるコードレビューを実施し、**指摘13件すべてに対応**しました（全面採用9件・部分採用4件）。
致命的4件（ロック未確認・なりすまし登録・秘密キー未設定時の全受理・LINE 409の誤判定）を含みます。
仕分けの理由と対応内容は `docs/レビュー対応ログ.md` を参照してください。

## 6. 未実施・今後

- 実機での動作確認（上記4章）
- 運用・監査観点のレビューは実施済み（`docs/運用監査レビュー.md`）。要判断は藤原様＋新任のサービス管理責任者へ
- Phase2（支援記録）はS3で全項目を無効化済み。利用者登録と項目名の確定後に有効化します
